#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<int> szczyty(n);
    for (int i = 0; i < n; i++) {
        cin >> szczyty[i];
    }

    int najwyzszy = *max_element(szczyty.begin(), szczyty.end());
    cout << najwyzszy << "\n";

    return 0;
}